<!-- ownership: kit -->
# Projects

Finite work organized around an `outcome`. Record `source_of_truth` as `internal` or a path/URL. Agents may sync verified status from that source; outcome or scope changes require a Proposal.
