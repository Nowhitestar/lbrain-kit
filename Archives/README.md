<!-- ownership: kit -->
# Archives

Inactive material retains its semantic role. Move it to the matching subdirectory instead of flattening it or hard-deleting it. Restore archived material only after user approval when the permission contract requires it.
