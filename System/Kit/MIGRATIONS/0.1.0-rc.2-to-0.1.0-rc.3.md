<!-- ownership: kit -->
# 0.1.0-rc.2 to 0.1.0-rc.3

No Seeded or User-owned content changes are required. This release corrects the Kit test fixture so personalized repositories with active Personal Skills can run the full suite.

Validate with:

```sh
python3 System/Kit/check.py
python3 -m unittest discover -s System/Kit/tests -v
```
