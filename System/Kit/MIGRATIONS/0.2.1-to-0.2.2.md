<!-- ownership: kit -->
# 0.2.1 to 0.2.2

This patch changes only Context Pack test fixtures. Existing public Skill and Pack submodules no longer create false failures when the Kit test suite verifies that preview operations leave Git configuration unchanged.

No Personal content migration is required. Follow the standard tagged-release workflow in [[System/Kit/SETUP]] and merge `v0.2.2` into personal `main`.
