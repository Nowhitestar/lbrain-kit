<!-- ownership: kit -->
# 0.1.0-rc.1 to 0.1.0-rc.2

No Seeded or User-owned content changes are required.

After merging the release tag, symlink installations immediately use the updated Skill Manager. Copy-mode users may rerun the installer: identical copies are skipped, while a changed existing copy is reported as a conflict and must be reviewed before replacement.

Validate with:

```sh
python3 System/Kit/check.py
python3 -m unittest discover -s System/Kit/tests -v
```
