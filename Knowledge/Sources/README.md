<!-- ownership: kit -->
# Sources

Store one source record per durable input. Preserve the author's meaning and provenance; do not silently rewrite raw excerpts. Use `capture: reference`, `excerpt`, or `full`, and set `weaving` to `pending`, `woven`, or `skip`.

Agents may create source notes and repair metadata directly. Material changes to captured source text require explicit approval.
